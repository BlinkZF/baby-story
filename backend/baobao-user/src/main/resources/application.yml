server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/baobao?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000ms

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: assign_uuid

jwt:
  secret: baobao-secret-key-2026-very-long-enough
  expire-days: 30

sms:
  # 开发模式：验证码固定为 123456，不真实发送
  mock: true
